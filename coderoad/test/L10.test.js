/*const assert = require('assert');
const { getLastCommand, getCwd } = require('./utils');

describe('tests', () => {
  let cwd, lastCommand;
  before(async () => {
    lastCommand = await getLastCommand();
    cwm = await getCwd();
  });

  it('should pass', async () => {
    assert(lastCommand[0] === 'pwd' && cwd === '/home/strove/project');
  });
});*/

// solution
